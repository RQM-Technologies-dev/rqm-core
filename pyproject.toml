[build-system]
requires = ["setuptools>=68", "wheel", "setuptools-scm>=8"]
build-backend = "setuptools.build_meta"

[project]
name = "rqm-core"
dynamic = ["version"]
description = "Core quaternion, spinor, SU(2), and Bloch mathematics for the RQM Python ecosystem"
authors = [{ name = "RQM Technologies" }]
readme = "README.md"
license = { file = "LICENSE" }
requires-python = ">=3.10"
dependencies = ["numpy>=1.24"]
keywords = [
    "quaternion",
    "su2",
    "bloch sphere",
    "spinor",
    "quantum",
    "linear algebra",
    "rotation",
    "mathematics",
]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Science/Research",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Topic :: Scientific/Engineering :: Mathematics",
    "Topic :: Scientific/Engineering :: Physics",
    "Typing :: Typed",
]

[project.optional-dependencies]
dev = ["pytest>=7.0", "pytest-cov>=4.0"]

[project.urls]
"Homepage" = "https://rqmtechnologies.com"
"Documentation" = "https://docs.rqmtechnologies.com"
"Source" = "https://github.com/RQM-Technologies-dev/rqm-core"
"Issues" = "https://github.com/RQM-Technologies-dev/rqm-core/issues"

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
rqm_core = ["py.typed"]

[tool.setuptools_scm]
version_scheme = "release-branch-semver"
local_scheme = "no-local-version"
tag_regex = "^v(?P<version>\\d+\\.\\d+\\.\\d+)$"

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "--tb=short"